import { DynamicDatePipePipe } from './dynamic-date-pipe.pipe';

describe('DynamicDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
